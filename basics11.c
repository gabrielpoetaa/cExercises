#include <stdio.h>
#include <stdlib.h>

int main()
{
{

    float value = 2.50, res = value * value * value;
    printf ("The result is: %10.2f\n", res);

}
{
    printf("The result is: %10.2f", pow(2.5, 3)); // the exercise in the book asked to use the pow function
}
}
