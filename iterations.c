#include <stdio.h>
#include <stdlib.h>

int main()
{
    int index;
    index = 1;
    while (index <= 10)
           {
           printf("%d", index);
           index = index + 1;
           }

    return 0;
}
